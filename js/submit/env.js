// Committed inactive stub. A deploy workflow overwrites the deployed copy
// from CI secrets/vars when they are set. Never commit real values here.

export const SUBMIT_ENV = { enabled: false, brokerUrl: '', token: '' };
